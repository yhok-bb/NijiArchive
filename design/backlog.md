# バックログ（未着手の課題・検討事項）

issueは着手直前に切る運用（CLAUDE.md）のため、将来の課題はまずここに記録する。
各Stepの着手時にこの一覧を見返し、拾うものをissue化する。完了したら行を消す。

| 課題 | 背景・なぜ問題か | 再訪タイミング候補 | MVP内/外 |
|------|----------------|------------------|---------|
| pending孤児の掃除・救済ジョブ | タブ閉じ・切断でpendingのまま残るVideoレコードが発生する。R2に実体があるケース（PUT成功後・complete前に離脱）は救済価値がある。古いpendingを走査→head_objectで実在確認→あればuploaded化・なければ失敗扱い、を定期ジョブで行う。Solid Queue定期実行（recurring.yml）の初出になる | Step 1終盤〜Step 2前 | 内 |
| completeの冪等化 | 応答を受け損ねたクライアントの再送で、現状は409が返る。既にuploadedなら200を返す方がリトライと整合する。変更は小さい | #11のPR内でも可 | 内 |
| マルチパートアップロード対応 | 単発PUTは5 GiB上限（#10で調査済み）。本番想定の2時間ライブ（6〜10GB）は現状アップロード不可。また単発PUTは途中失敗で全やり直しになるため、大容量ではパート単位リトライが実用上必須。手順は#10のコメントに調査済み | Step 1完了後、大容量負荷試験の前 | 内（後半） |
| アップロード進捗表示 | fetchはアップロード進捗を取得できず、大容量転送中に画面が無応答に見える。XHR化またはマルチパートのパート単位進捗で解決する | マルチパート対応と同時 | 内（後半） |
| 大容量アップロードの負荷試験 | 実速度・失敗率・体感の計測。まず単発PUT上限内（2〜4GB）で実施し、マルチパート対応後に本番想定サイズで再実施する | Step 1完了後（DD-1と関連） | 内（後半） |
| Presigned URL期限切れ時の再発行リトライ | 発行から30分でURLが失効する。発行後すぐPUTする現フローでは踏みにくいが、失敗後の再試行フローを作る際に必要になる（Part 2の設計に記載あり） | マルチパート対応と同時 | 内（後半） |
| 管理画面UIの整備 | 現状は検証用の最小フォームのみ。タイトル入力、動画一覧、ready→publishedの公開操作（UC-4）が必要。公開時のtitle必須バリデーション（publishedへの遷移条件）もここで入れる | Step 1後半（視聴ページの前提） | 内 |
| R2クライアントの共通化 | 現在はAdmin::VideosControllerのprivateにある。#12で変換ジョブからもR2アクセスが必要になった時点で共通クラスに切り出す | #12着手時 | 内 |
| Video.lastの並び順（UUID主キー） | UUIDはid順＝作成順にならないため、`implicit_order_column = "created_at"` の設定を検討する。一覧実装で並びが必要になった時点でよい | 管理画面UI整備時 | 内 |
| 再変換・動画差し替え | 変換設定の変更や素材差し替え時に必要になる。ready→transcodingへの逆行遷移の解禁と、公開中セグメントを消さないためのバージョン付きパス（#7参照）がセットで必要 | 必要になった時点（MVP対象外） | 外 |
